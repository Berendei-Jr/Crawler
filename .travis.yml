language: cpp
compiler:
  - clang
os:
  - linux

addons:
  apt:
    packages:
      - g++
      - cmake

jobs:
  include:
    - name: "build"
      script:
        - mkdir _build && cd _build
        - cmake .. && make && ./crawler --help

notifications:
  email: false